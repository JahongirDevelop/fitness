server:
  port: 8080
  forward-headers-strategy: native


spring:
  datasource:
    url: jdbc:postgresql://${DB_IP:194.62.105.120}:${DB_PORT:5433}/${DB_NAME:fitness_database}
    username: ${DB_USERNAME:fitnessUser}
    password: ${DB_PASSWORD:1}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true





  mail:
    host: smtp.gmail.com
    port: 587
    username: coursemanagmentt@gmail.com
    password: rmtdqcbpmqabptsc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger
    tags-sorter: alpha
    operations-sorter: alpha
    disable-swagger-default-url: true
    doc-expansion: none



jwt:
  access-expiry: 86400000  # a day in milliseconds
  refresh-expiry: 604800000  # 7 days in milliseconds
  secret: gjhkoiutrefdsavzbxcnmktjyhretgwedfvbnjkrjyerhsfg



